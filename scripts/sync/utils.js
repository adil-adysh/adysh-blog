// Utilities: frontmatter parsing, asset path rewriting, slug generation

const fs = require('fs')

function parseFrontmatter(md) {
  // minimal placeholder
  return { }
}

module.exports = { parseFrontmatter }
